/**
 * Admin configuration — single source of truth for admin-only settings.
 * Change the password here before deployment.
 */
export const ADMIN_PASSWORD = "Lakshya@123";
export const ADMIN_ROUTE = "#/lakshya-admin";
export const STORAGE_PREFIX = "gbhss";

/** Feature flags for backend readiness — set to true when connecting a real API */
export const USE_BACKEND = false;
export const API_BASE = "/api"; // placeholder
